{
  "version": 2,
  "buildCommand": "NODE_ENV=production pnpm install --prod --no-frozen-lockfile && pnpm run build",
  "installCommand": "npm install -g pnpm",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  }
} 
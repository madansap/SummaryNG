{
  "version": 2,
  "buildCommand": "NODE_ENV=production SKIP_NATIVE_PACKAGES=1 pnpm install --no-frozen-lockfile && pnpm run build",
  "installCommand": "npm install -g pnpm && SKIP_NATIVE_PACKAGES=1 pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production",
    "SKIP_NATIVE_PACKAGES": "1"
  }
} 
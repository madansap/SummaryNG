{
  "version": 2,
  "buildCommand": "NODE_ENV=production pnpm install && pnpm run build",
  "installCommand": "npm install -g pnpm && pnpm install",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  }
} 
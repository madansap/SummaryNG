{
  "version": 2,
  "buildCommand": "NODE_ENV=production pnpm install --no-frozen-lockfile && pnpm run build",
  "installCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  }
} 
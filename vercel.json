{
  "version": 2,
  "buildCommand": "NODE_ENV=production pnpm install --prod && pnpm run build",
  "installCommand": "npm install -g pnpm",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  }
} 